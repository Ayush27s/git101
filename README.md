git init(initialize git in project)
git add(<file_name> (add a file in source control))
git add . (to add all changes in source control )
git commit -m "<measseage>" (attach a label to changes)

git push -u origin <main_branch> | git push

create new branch
git checkout -b <new_branch>

change branch
git checkout <branch_name>


git branch